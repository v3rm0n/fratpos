# Fratpos setup using Ansible

- Install ansible
- Define inventory
- Run `ansible-galaxy role install -r requirements.yml` to install roles
- Run `ansible-galaxy collection install -r requirements.yml -p ./` to install collections
