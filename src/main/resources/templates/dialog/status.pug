.modal
    .modal-dialog
        .modal-content
            .modal-header
                button.close(type='button', ng-click='$hide()') &times;
                h4.modal-title Lisa/Muuda staatust
            .modal-body
                p.bg-danger(ng-show='error') Viga salvestamisel. Kontrolli andmete korrektsust!
                form.form-horizontal(role='form', ng-submit='save(status)')
                    .form-group
                        label.control-label.col-sm-2(for='statusName') Nimetus
                        .col-sm-5
                            input.form-control#statusName(type='text', placeholder='Staatus', ng-model='status.name', focus-me)
            .modal-footer
                button.btn.btn-danger(ng-show='status.id !== undefined', ng-click='delete(status)') Kustuta
                button.btn.btn-success(ng-click='save(status)') Salvesta